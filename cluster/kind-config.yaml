kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ${CLUSTER_NAME:-test-infra}
nodes:
  - role: control-plane
    image: kindest/node:${K8S_VERSION:-v1.30.0}
    extraPortMappings:
      # Envoy Gateway HTTPS
      - containerPort: 30443
        hostPort: 30443
        protocol: TCP
